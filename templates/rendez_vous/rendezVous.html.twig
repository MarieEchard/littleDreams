{% extends 'base.html.twig' %}



{% block title %}Rendez-vous - LittleDreams{% endblock %}

{% block body %}

    <form method="post">
        <div class="background">
            <img src="{{ asset('img/Wallpaper IA.png') }}" alt="image IA" style="height: 100%; width: 100%">
        </div>
        <div class="d-flex justify-content-center">
            <div class="border border-0 rounded p-4 form-custom">
                <h3 class="display-6 text-center">Demander un Rendez-vous</h3>
                {{ form_start(form) }}
                {{ form_row(form.dateHeure) }}
                {{ form_row(form.raison) }}
                {{ form_widget(form.submit) }} {{ form_widget(form.return) }}
                {{ form_end(form) }}
            </div>
        </div>
    </form>
{% endblock %}
